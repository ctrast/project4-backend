<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
   
    
    
    <h4>Ingredients</h4>
    <ul>
        <% category.phrases.forEach(ing => { %>
         <li>
           <%= ing.name %>
           <form action="/<%=ing._id%>/<%=category._id%>?_method=DELETE" method="POST">
            <button>Delete</button>
          </form>
          </li>
        <% }) %>
    </ul>

</body>
</html>